.button-row
  .button_strip
    %ul.button-group.round.right
      - if @meal_ingredient_buttons.include?(:save)
        %li= link_to(icon('save',"Save"), @meal_ingredient, class: 'button submit')

      - if @meal_ingredient_buttons.include?(:cancel)
        %li= link_to(icon('undo',"Cancel"), session[:meal_ingredient_return_to], class: 'button')

      - if @meal_ingredient_buttons.include?(:delete)
        %li= link_to(icon('trash',"Delete"), @meal_ingredient, class: 'button', data: {confirm: 'Are you sure?'}, method: :delete)

  %h1= @meal_ingredient_page_heading.gsub(/\s/, '&nbsp;').html_safe