.form
  = render partial: 'button_strip'
  = render partial: '/food_menus/collections/show', locals: {id: "collection_#{@meal.id}", collection: @meal.collection, hidden_initially: true, show_prefix: false}